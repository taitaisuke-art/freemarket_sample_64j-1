.step-1

  .step-1_header
    .step-1_header__icon
      = link_to root_path do
        = image_tag "fmarket_logo_red.svg", class: "header_logo"

    %nav
      %ul
        %li-ok
          会員登録
          .nav-dot-ok
          .nav-line-ok
        %li
          電話番号認証
          .nav-dot
        %li
          お届け先住所入力
          .nav-dot
        %li
          支払い方法
          .nav-dot
        %li
          完了
          .nav-dot

  .form-1
    %h1
      会員情報入力
    .form-user
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        = render "devise/shared/error_messages", resource: resource

        .form-user__nickname
          = f.label :ニックネーム
          %span.form-require
            必須
          %br/
          = f.text_field :nickname, placeholder: "例）メルカリ太郎", class: "input-default"

        .form-user__email
          = f.label :メールアドレス
          %span.form-require
            必須
          %br/
          = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "PC・携帯どちらでも可", class: "input-default"

        .form-user__password
          = f.label :パスワード
          %span.form-require
            必須
          - if @minimum_password_length
            %em
              (#{@minimum_password_length} characters minimum)
          %br/
          = f.password_field :password, autocomplete: "new-password", placeholder: "7文字以上の半角英数字", class: "input-default"
          %p
          ※ 英字と数字の両方を含めて設定してください
          

        .form-user__password
          = f.label :確認用パスワード
          %span.form-require
            必須
          %br/
          = f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "7文字以上の半角英数字", class: "input-default"
          %p
          ※ 英字と数字の両方を含めて設定してください

        .form-user__identification
          本人確認
          %p
            安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。

        .form-user__name
          = f.label :お名前（全角）
          %span.form-require
            必須
          .input-name
            = f.text_field :last_name, placeholder: "例)山田", class: "input-default"

            = f.text_field :first_name, placeholder: "例)彩", class: "input-default__lastname"

        .form-user__name-kana
          = f.label :お名前カナ（全角）
          %span.form-require
            必須
          .input-name
            = f.text_field :last_name_kana, placeholder: "例)ヤマダ", class: "input-default"
            = f.text_field :first_name_kana, placeholder: "例)アヤ", class: "input-default__lastname"

        .form-user__birthday
          = f.label :生年月日
          %span.form-require
            必須
          %br/
          = f.text_field :birthyear, placeholder: "1989（年）", class: "input-default"

          = f.text_field :birthmonth, placeholder: "02（月）", class: "input-default"

          = f.text_field :birthday, placeholder: "18（日）", class: "input-default"

        .form-user__birthday
          = f.label :携帯電話の番号
          %span.form-require
            必須
          %br/
          = f.text_field :telephone, placeholder: "携帯電話の番号を入力", class: "input-default"
          %p
          ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。

        .form-user__text
          「次へ進む」のボタンを押すことにより、利用規約に同意したものとみなします
          = f.submit "次へ進む", class: "submit"

          .form-user__text2
            %p
              本人情報の登録について
            = icon "fas fa-angle-right", class: "fas fa-angle-right"
  //= render "devise/shared/links"
  

  .step-1_fooder
    %nav
      %ul
        %li
          = link_to "プライバシーポリシー", root_path
        %li
          = link_to "フリーマケット利用規約", root_path
        %li
          = link_to "特定商取引に関する表記", root_path
            

    .fooder-image
      = link_to root_path do
        = image_tag "logo_white.svg", class: "fooder_logo"

    %p
      &copy; Fmarket 64 teamJ